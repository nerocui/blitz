namespace Blitz.WinUI
{
    /// ABI exposed to C#
    runtimeclass Host
    {
        // Accept a XAML element (e.g., Microsoft.UI.Xaml.Controls.SwapChainPanel) as an Object.
        // We'll QI to the native interop interface inside the component.
        Host(Object panel, UInt32 width, UInt32 height, Single scale);
        void SetPanel(Object panel);
        void Resize(UInt32 width, UInt32 height, Single scale);
        void RenderOnce();
        void LoadHtml(String html);
    }
}
